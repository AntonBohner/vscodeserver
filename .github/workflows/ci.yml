name: CI

on:
  push:
  pull_request:

jobs:
  syntax:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: zsh syntax
        run: |
          zsh -n bin/vscodeserver
          zsh -n install.sh
